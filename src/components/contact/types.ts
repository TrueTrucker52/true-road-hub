export type GeneralContactValues = {
  name: string;
  email: string;
  message: string;
};

export type BrandInquiryValues = {
  company: string;
  contact: string;
  email: string;
  budget: string;
  details: string;
};